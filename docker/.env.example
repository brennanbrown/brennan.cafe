# ============================================================================
# ENVIRONMENT VARIABLES - brennan.cafe Homelab
# ============================================================================
# Description: Environment variables for Docker services
# Location: ~/brennan.cafe/docker/.env
# Usage: Copy this file to .env and fill in your actual values
#
# IMPORTANT: Never commit the .env file to git!
# ============================================================================

# ============================================================================
# NEXTCLOUD
# ============================================================================

# Database password for Nextcloud PostgreSQL
# Generate with: openssl rand -base64 32
NEXTCLOUD_DB_PASSWORD=change_me_random_password_here

# Admin password for Nextcloud web interface
# Use a strong password!
NEXTCLOUD_ADMIN_PASSWORD=change_me_admin_password_here

# ============================================================================
# HEDGEDOC
# ============================================================================

# Database password for HedgeDoc PostgreSQL
# Generate with: openssl rand -base64 32
HEDGEDOC_DB_PASSWORD=change_me_random_password_here

# Session secret for HedgeDoc
# Generate with: openssl rand -hex 64
HEDGEDOC_SESSION_SECRET=change_me_session_secret_here

# ============================================================================
# PLAUSIBLE ANALYTICS
# ============================================================================

# Database password for Plausible PostgreSQL
# Generate with: openssl rand -base64 32
PLAUSIBLE_DB_PASSWORD=change_me_random_password_here

# Secret key base for Plausible
# Generate with: openssl rand -base64 64
PLAUSIBLE_SECRET_KEY=change_me_secret_key_here

# ============================================================================
# CLOUDFLARE (for Cloudflare Tunnel)
# ============================================================================

# Your Cloudflare tunnel token
# Get this from: https://dash.cloudflare.com/
# CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here

# ============================================================================
# TIMEZONE
# ============================================================================

# Your local timezone (for logs and scheduling)
TZ=America/Edmonton

# ============================================================================
# GENERAL SETTINGS
# ============================================================================

# Your email for notifications and SSL certificates
ADMIN_EMAIL=mail@brennanbrown.ca

# Domain name
DOMAIN=brennan.cafe

# ============================================================================
# BACKUP CONFIGURATION (Optional)
# ============================================================================

# Restic backup password
# Generate with: openssl rand -base64 32
# RESTIC_PASSWORD=change_me_backup_password_here

# Backup destination (local path or remote)
# BACKUP_DESTINATION=/media/brennan/backups

# ============================================================================
# NOTES
# ============================================================================

# To generate secure random passwords, use:
#   openssl rand -base64 32    # For passwords
#   openssl rand -hex 64       # For session secrets
#   uuidgen                    # For UUIDs
#
# Keep this file secure:
#   chmod 600 .env
#
# This file should NEVER be committed to git!
# The .gitignore file should include:
#   .env
#   *.env
#   !.env.example
#
# After creating your .env file, verify it works:
#   docker compose config
#
# This will show you the final configuration with variables substituted.